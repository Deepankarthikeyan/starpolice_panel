PORT=5000
MONGODB_URI=mongodb://127.0.0.1:27017/starpolice_academy
JWT_SECRET=change-this-secret-in-production
CLIENT_URL=http://localhost:5173

# Email (optional in dev — without SMTP, emails are logged to the API console)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=noreply@starpolice.academy
