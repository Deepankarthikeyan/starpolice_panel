[build]
  base = "package"
  command = "npm ci && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA routing for React Router (/admin/dashboard, /student/login, etc.)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
