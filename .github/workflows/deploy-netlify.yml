name: Deploy frontend to Netlify

on:
  push:
    branches: [master]
    paths:
      - "package/**"
      - "netlify.toml"
      - ".github/workflows/deploy-netlify.yml"
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: package/package-lock.json

      - name: Install and build
        working-directory: package
        env:
          VITE_API_URL: ${{ secrets.VITE_API_URL }}
        run: |
          npm ci
          npm run build

      - name: Verify Netlify credentials
        id: netlify_creds
        run: |
          if [ -z "${{ secrets.NETLIFY_AUTH_TOKEN }}" ] || [ -z "${{ secrets.NETLIFY_SITE_ID }}" ]; then
            echo "::warning::NETLIFY_AUTH_TOKEN and NETLIFY_SITE_ID are not set. Skipping Netlify deploy."
            echo "Add secrets at: https://github.com/Deepankarthikeyan/starpolice_panel/settings/secrets/actions"
            echo "Or trigger a deploy from the Netlify dashboard for site starpolice-panel."
            echo "ready=false" >> "$GITHUB_OUTPUT"
          else
            echo "ready=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Deploy to Netlify
        if: steps.netlify_creds.outputs.ready == 'true'
        uses: nwtgck/actions-netlify@v3.0
        with:
          publish-dir: "./package/dist"
          production-branch: master
          github-token: ${{ secrets.GITHUB_TOKEN }}
          deploy-message: "Deploy from GitHub Actions"
        env:
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
          NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}
        timeout-minutes: 5
